# MongoDB connection string (set in Vercel dashboard)
MONGODB_URI=mongodb://localhost:27017/rentlona

# Port for the server (Vercel sets this automatically, but can override)
PORT=5000

# Frontend URL for CORS (set to your Vercel frontend URL)
FRONTEND_URL=http://localhost:3000

# JWT secret for authentication (generate a secure random string)
JWT_SECRET=your_jwt_secret_here
